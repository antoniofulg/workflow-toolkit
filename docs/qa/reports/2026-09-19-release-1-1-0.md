# Workflow Toolkit 1.1.0 Release QA Execute

- **Date:** 2026-09-19
- **Frozen tree:** `ea132ad32c1e1ff9d074ca9535d24102137e05fd`
- **Result:** PASS — local release identity, archive, packaged CLI, reload, and residue matched
- **Adapter:** CLI/manual through the packed public `wtk` executable plus independent filesystem readback
- **Execution path:** `CHANGELOG.md`; `README.md`; `package.json`; `bun.lock`; local archive; disposable Git consumer
- **Environment:** Darwin 27.0.0 arm64; Node v22.23.1; Bun 1.4.2; Python 3.14.7; Git 2.54.0; Expect 5.45
- **Recorded gate:** reused same-snapshot `bun run test:all` — exit `0`; 127 Bun, 208 Node, and 275 Python results; 0 failures
- **Raw evidence:** `docs/qa/evidence/2026-09-19-release-1-1-0/`

## Matrix

| Charter | Scenario | Verdict | Independent confirmation | Evidence |
| --- | --- | --- | --- | --- |
| `CH-review-release-1-1-0-2026-09-19` | `REL-report-current-workflow-release` | pass | Extracted manifest, installed adoption manifest, exact Jev bytes, and a fresh no-change CLI run independently confirmed the source claims | `release-readback.md` |

## Release identity

The newest changelog heading, source and extracted package manifests, README release identity, and
source and extracted installer constants agree on `workflow-toolkit@1.1.0`. Both manifests expose
only `wtk -> bin/wtk.js`. The changelog migration command is
`npx workflow-toolkit@1.1.0 install`. Bun 1.4's lockfile identifies root package
`workflow-toolkit` and runtime dependency `smol-toml@1.8.0`; it intentionally omits a duplicate
root-version field.

## Exact package

`bun pm pack --filename /Users/antoniofulg/Projects/my-workflow/docs/qa/evidence/2026-09-19-release-1-1-0/pack/workflow-toolkit-1.1.0.tgz --ignore-scripts`
exited `0`. The archive has 146 members and SHA-256
`71a5a232682ef1ebf7b47779d831655e2538d2e1995be766a8e07de931bd6127`.

The member list contains `package/bin/wtk.js`, the owning QA Execute skill, and
`package/.agents/skills/wtk-qa-execute/jev_adapter.py`. No member matched `.env`, `qa.env`, a
credential/secret filename, `.git`, `node_modules`, `docs/qa/evidence`, `.wtk-deep-review`,
`.specs`, `__pycache__`, or `*.pyc`.

## Packaged CLI and independent reload

The extracted public CLI exposed `wtk install`. From a clean disposable Git consumer, Expect chose
module `2`, confirmed preview and apply, and observed `Installation complete.` for `core, quality`
with 160 additions. A separate process reloaded schema `1`, workflow version `1.1.0`, layers
`core,quality`, 126 managed files, and 3 blocks from `.my-workflow/adoption.json`.

Packed and installed Jev helpers matched byte-for-byte at SHA-256
`84748996d1f7efd8223be104dae1bf42af413abc0b7bb7902a767dab31cebce3`; their owning skill copies
also matched. The installed policy retained process-only credential handling, dedicated headless
default, explicit-headed allowance, Playwright-MCP-first fallback, Orca/Maestri/manual order, and
the independent-oracle boundary. No provider or browser ran. No environment file, external
security skill, or package dependency tree appeared in the consumer.

After the installed fixture was committed, a new CLI process reloaded the consumer and reported
`Selected modules are up to date. No files will change.` The consumer stayed clean. The existing
feature-level report `docs/qa/reports/2026-09-19-optional-jev-qa-adapter.md` remains the behavioral
authority for safe Jev preflight and fallback metadata; this release walk did not repeat it.

## Gate selection

The coordinator supplied a same-snapshot gate receipt for clean HEAD `ea132ad3`: `bun run
test:all`, exit `0`, about 91.1 seconds, 127 Bun tests with 1085 expectations, 208 Node tests, and
275 Python results, all with zero failures and no tracked changes. Product code, dependencies,
configuration, command, and runtime inputs stayed equivalent, so that result was reused under
incremental validation. This QA cycle then changed only its durable charter, report, and scenario;
the scoped release/history contract ran afterward:

- `bun test tools/shared/tests/qa-skills.test.ts -t 'IT-005 / AIM-11 reports release version and Bun lock identity consistently'`
  — exit `0`; 1 pass, 0 failures.
- `git diff --check` and the trailing-whitespace scan — exit `0`; no findings.

## Findings

No release product defect was observed and no bug record was created. The durable journey's first
flow step still names historical release `1.0.0`; this charter and version-neutral scenario supplied
the current `1.1.0` scope. That stale planning prose is not shipped in the package and did not alter
the observed release surface.

## Cleanup

The exact archive, extraction, and disposable consumer roots were removed after evidence capture
and confirmed absent. Final source status differs from the opening clean snapshot only by this
cycle's charter, report, and scenario update.

## Limitations

Registry/tag consistency is outside this local-only cycle. No registry/network, publication, tag,
push, pull request, merge, deploy, production mutation, external skill installation, provider call,
secret read, or browser run is authorized.
